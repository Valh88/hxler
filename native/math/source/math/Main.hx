package math;

import math.Entry;

/**
 * Dummy main (DLL build needs a valid -main; the NIF entrypoint lives in
 * the generated glue of math.Entry).
 */
@:keep
class Main {
	public static function main() {}
}
